{
    "version": "$cnm_schema_version",
    "submissionTime": "$submission_time",
    "identifier": "$uuid",
    "collection": "$auth_id",
    "provider": "$provider",
    "product": {
        "name":  "$producer_granule_id",
        "dataVersion": "$version",
        "files": $file_content
    }
}
