[project]
name = "xboxmadeeasy"
version = "0.1.0"
description = "Plug-and-play Xbox controller input tracking for Python"
authors = [{ name = "Wilder", email = "sa.yound@gmail.com" }]
readme = "README.md"
license = { file = "LICENSE" }
dependencies = []

[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"