[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "py-cli-agent"
version = "0.1.2"
description = "A CLI Agent powered by Gemini AI"
readme = "README.md"
authors = [
  { name="Andro", email="androkingdom1@gmail.com" }
]
license-files = ["LICENSE"]
dependencies = [
  "rich",
]
[project.scripts]
  andro-cli = "cli_agent.main:app"

[project.urls]
"Homepage" = "https://github.com/andro-dev/cli-agent"
"Bug Tracker" = "https://github.com/andro-dev/cli-agent/issues"
