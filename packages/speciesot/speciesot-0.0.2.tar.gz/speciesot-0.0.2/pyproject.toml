[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "speciesot"
version = "0.0.2"
description = "A software package for cross-species transcriptome analysis using GWOT"
authors = [
  { name="Yuya Tokuta", email="yuyatokuta@gmail.com" }
]
readme = "README.md"
requires-python = ">=3.9"
license = {text = "BSD-3-Clause"}

