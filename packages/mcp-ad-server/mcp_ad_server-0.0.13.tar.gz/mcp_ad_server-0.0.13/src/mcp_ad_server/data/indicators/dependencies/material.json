{
  "endpoint": "GetMaterialCountList",
  "version": "1.0.0",
  "updated_at": "2025-08-19",
  "notes": "中文指标依赖规则（素材）。当前主要提供与累计类相关的触发规则；TT 路由已自动包含‘素材id’，此处不再强制注入。",
  "rules": [
    { "indicator": "分成后累计ROI", "requires": ["累计ROI"] },
    { "indicator": "付费分成后累计ROI", "requires": ["累计ROI"] }
  ]
}
