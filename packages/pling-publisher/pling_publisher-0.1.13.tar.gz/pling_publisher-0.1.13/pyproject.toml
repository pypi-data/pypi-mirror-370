[tool.poetry]
name = "pling-publisher"
version = "0.1.13"
description = ""
readme = "README.md"
repository = "https://github.com/dmzoneill/pling-publisher"
authors = ["David O Neill <dmz.oneill@gmail.com>"]


[tool.poetry.dependencies]
python = "^3.8"
requests = "*"
typer = "*"

[tool.poetry.dev-dependencies]
pytest = "^6.0.2"

[tool.poetry.scripts]
pling = "pling_publisher.cli:app"

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
