[build-system]
requires = ["poetry-core>=2.0.0"]
build-backend = "poetry.core.masonry.api"

[project]
name = "tracerec"
version = "0.3.0"
description = "Python package for the creation of recommendation systems based on user traces."
authors = [
    {name = "Alex Martínez", email = "almtav08@gmail.com"}
]
readme = "README.md"
license = {text = "MIT"}
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
]
requires-python = ">=3.8"
dependencies = [
    "numpy>=1.23.0",
    "torch>=2.0.0"
]

[project.urls]
"Homepage" = "https://github.com/almtav08/tracerec"
"Bug Tracker" = "https://github.com/almtav08/tracerec/issues"

[tool.poetry.group.dev.dependencies]
pytest = "^7.4.0"
black = "^23.7.0"
isort = "^5.12.0"
