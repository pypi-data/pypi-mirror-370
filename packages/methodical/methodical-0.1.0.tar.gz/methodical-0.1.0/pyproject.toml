[build-system]
requires = ["setuptools>=80.9"]
build-backend = "setuptools.build_meta"

[project]
name = "methodical"
version = "0.1.0"
description = "Scan a markdown file and create a table of content"
readme = "README.md"
requires-python = ">=3.12"
license = { text = "MIT" }
authors = [
  { name = "LaLinguette", email = "LaLinguette@gmail.com" }
]


[project.urls]
Homepage = "https://github.com/lalinguette/methodical"
