codecov:
    notify:
        # do not notify until at least 12 builds (python versions * os * extension = 3 * 2 * 2 = 12) have been uploaded from the CI pipeline
        after_n_builds: 12 # https://docs.codecov.com/docs/codecovyml-reference#codecovnotify
