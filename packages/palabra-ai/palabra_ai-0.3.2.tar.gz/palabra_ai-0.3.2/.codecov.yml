coverage:
  status:
    project:
      default:
        target: 60%  # Target coverage percentage
        threshold: 10%  # Allow 10% drop from target
        paths:
          - "src/*"
        ignore:
          - "src/palabra_ai/experimental/**/*"
    patch:
      default:
        target: 60%  # Target for new code
        threshold: 10%  # Allow 10% drop from target
        paths:
          - "src/*"
        ignore:
          - "src/palabra_ai/experimental/**/*"