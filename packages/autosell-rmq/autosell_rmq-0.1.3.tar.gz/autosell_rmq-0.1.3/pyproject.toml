[project]
name = "autosell-rmq"
version = "0.1.3"
description = ""
authors = [
    {name = "Zhanibek",email = "rzhanik@gmail.com"}
]
exclude = [
    "local"
]
readme = "README.md"
requires-python = ">=3.11,<4.0.0"
dependencies = [
    "aio-pika (>=9.5.5,<10.0.0)"
]

[tool.poetry]
packages = [{include = "autosell_rmq"}]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
