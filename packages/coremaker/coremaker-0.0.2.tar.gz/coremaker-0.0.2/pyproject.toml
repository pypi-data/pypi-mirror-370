[project]
name = "coremaker"
version = "0.0.2"
description = ""
authors = [
    {name = "Coremaker",email = "team@coremaker.io"}
]
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
]

[tool.poetry]
packages = [{include = "coremaker", from = "src"}]

[tool.poetry.scripts]
coremaker = "coremaker.main:cli"


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
