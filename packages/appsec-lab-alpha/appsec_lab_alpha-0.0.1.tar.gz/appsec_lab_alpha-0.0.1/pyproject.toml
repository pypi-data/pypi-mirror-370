[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "appsec-lab-alpha"
version = "0.0.1"
description = "Package name reserved by Schibsted Application Security Team"
authors = [{ name = "media-appsec", email = "media-appsec@schibsted.com" }]
readme = "README.md"
license = {text = "MIT"}
keywords = ["security", "dependency confusion", "placeholder"]