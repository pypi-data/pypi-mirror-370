[tool.poetry]
name = "deckard"
version = "0.1.0"
description = "Extract structured data from unstructured text — no AI, just regular expressions. 🔍"
license = "MIT"
authors = ["SpaceShaman <spaceshaman@tuta.io>"]
readme = "README.md"
homepage = "https://github.com/SpaceShaman/deckard"
repository = "https://github.com/SpaceShaman/deckard"
documentation = "https://github.com/SpaceShaman/deckard"
keywords = ["regex", "data-extraction", "regular-expression"]

[tool.poetry.dependencies]
python = "^3.10"
regex = "^2025"

[tool.poetry.group.dev.dependencies]
pytest = "^8"
pytest-cov = "^5"
debugpy = "^1"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
