services:
  script:
    env_file:
      - .env
    build:
      context: .
      dockerfile: Dockerfile
      target: python
      args:
        WORKDIR: ${WORKDIR}
    networks:
      - ties
networks:
  ties:
    external: true
