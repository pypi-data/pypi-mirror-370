[project]
name = "ly-bigquery-plugin"  # 结合你的用户名和功能描述
version = "0.1.2"  # 同时升级版本号
authors = [{ name = "刘渊", email = "yuan.liu@pandasofcaribbean.com" }]
requires-python = ">=3.8"


[build-system]
requires = ["poetry-core", "setuptools>=61.0", "wheel"]  # 增加 setuptools 依赖
build-backend = "setuptools.build_meta"


