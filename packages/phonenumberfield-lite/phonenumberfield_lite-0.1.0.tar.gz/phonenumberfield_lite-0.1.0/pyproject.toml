[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "phonenumberfield_lite"
version = "0.1.0"
description = "Lightweight phone number field for Django"
authors = [
    { name="Amirhoseinheidari", email="amirhosein.hydri1381@email.com" }
]
dependencies = ["Django>=3.2", "phonenumbers>=8.13"]
readme = "README.md"

[tool.setuptools.packages.find]
where = ["."]
