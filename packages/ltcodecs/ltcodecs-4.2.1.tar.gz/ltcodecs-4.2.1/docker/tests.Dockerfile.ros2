ARG BASE_IMAGE=ros:jazzy-ros-base
FROM ${BASE_IMAGE}

RUN apt-get update && apt-get upgrade -y
RUN apt install python3-pip python3-venv git -y

RUN python3 -m venv /opt/venv
ENV PATH="/opt/venv/bin:$PATH"

RUN pip install -U pytest pytest-cov lark argcomplete numpy psutil
