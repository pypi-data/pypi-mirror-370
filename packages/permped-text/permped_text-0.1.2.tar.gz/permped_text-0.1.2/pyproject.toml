[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "permped-text"
version = "0.1.2"
description = "A package for text processing."
authors = [{ name = "Eli Torn" }]
readme = "README.md"
requires-python = ">=3.8"

[tool.setuptools.packages.find]
include = ["app"]
