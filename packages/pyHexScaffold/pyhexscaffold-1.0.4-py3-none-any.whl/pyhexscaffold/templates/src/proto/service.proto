syntax = "proto3";

// 服务定义示例
// 注意：这只是一个示例，需要根据实际需求进行修改

package service;

// 服务请求消息
message ServiceRequest {
  string data = 1;
}

// 服务响应消息
message ServiceResponse {
  string result = 1;
  bool success = 2;
}

// 定义服务接口
service Service {
  // 服务方法示例
  rpc ProcessData (ServiceRequest) returns (ServiceResponse);
}
