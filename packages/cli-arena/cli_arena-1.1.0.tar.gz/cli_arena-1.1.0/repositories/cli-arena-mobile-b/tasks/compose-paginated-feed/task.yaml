id: compose-paginated-feed
title: Build Paginated Feed with LazyColumn
description: 'Implement a paginated scrolling feed using LazyColumn and rememberLazyListState().

  Data should be loaded in pages via ViewModel, with a loading spinner shown at the end.

  Use mocked data (no network).

  '
author_email: your-email@domain.com
difficulty: medium
estimated_time: 45
tags:
- pagination
- lazycolumn
- compose
- state
files_to_modify:
- src/viewmodel/FeedViewModel.kt
- src/ui/screens/FeedScreen.kt
- tests/FeedPaginationTest.kt
success_criteria:
- Items load only on scroll
- Footer loader shows while fetching
- List state preserved during rotation
environment:
  os: ubuntu-24.04
  os_name: Ubuntu 24.04 LTS
  architecture: x86_64
  container: docker
  runtimes:
    python: 3.12.3
    node: 20.x
    go: 1.21.5
    terraform: 1.5.7
  tools:
  - git
  - curl
  - wget
  - sqlite3
  - build-essential
  - tmux
  - asciinema
  - unzip
  note: Tasks run in isolated Docker containers based on Ubuntu 24.04
