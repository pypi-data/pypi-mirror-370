id: background-sync-worker
title: Add SyncWorker with Retry Policy
description: 'Add a periodic WorkManager task to sync local DB with server every 24 hours.

  Handle failure retry with exponential backoff.


  Requirements:

  - Implement CoroutineWorker

  - Retry on network failure

  - Log executions with timestamps

  '
author_email: your-email@domain.com
difficulty: hard
estimated_time: 60
tags:
- worker
- background
- coroutine
- retry
- logging
files_to_modify:
- app/src/main/java/com/project/saturn/workers/SyncWorker.kt
- app/src/main/AndroidManifest.xml
- app/src/test/java/com/project/saturn/SyncWorkerTest.kt
success_criteria:
- Worker executes on schedule
- Retry triggered on failure
- Logs show correct sync attempt metadata
environment:
  os: ubuntu-24.04
  os_name: Ubuntu 24.04 LTS
  architecture: x86_64
  container: docker
  runtimes:
    python: 3.12.3
    node: 20.x
    go: 1.21.5
    terraform: 1.5.7
  tools:
  - git
  - curl
  - wget
  - sqlite3
  - build-essential
  - tmux
  - asciinema
  - unzip
  note: Tasks run in isolated Docker containers based on Ubuntu 24.04
