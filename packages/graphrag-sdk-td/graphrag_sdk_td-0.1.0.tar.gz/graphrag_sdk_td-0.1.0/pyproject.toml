[project]
name = "graphrag-sdk-td"
version = "0.1.0"
description = "Python SDK cho GraphRAG (chat + upload data)"
authors = [{name = "tungdao"}]
dependencies = ["requests"]

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"
