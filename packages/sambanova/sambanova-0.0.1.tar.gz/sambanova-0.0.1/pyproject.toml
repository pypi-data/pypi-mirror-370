[project]
name = "sambanova"
version = "0.0.1"
description = "Placeholder – package under construction"
authors = [
    {name = "sambanova",email = "jorge.piedrahita@sambanovasystems.com"}
]
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
]

[tool.poetry]
packages = [{include = "sambanova", from = "src"}]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
