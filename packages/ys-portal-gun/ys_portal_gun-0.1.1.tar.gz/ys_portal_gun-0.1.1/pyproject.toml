[project]
name = "ys-portal-gun"
version = "0.1.1"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = ["typer>=0.16.0"]


[project.scripts]
ys-portal-gun = "ys_portal_gun.main:app"

[build-system]
requires = ["uv_build>=0.8.11,<0.9.0"]
build-backend = "uv_build"

