[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "fastcoding"
version = "0.1.0"
description = "A Python package for fast and structured coding utilities"
readme = "README.md"
license = { text = "MIT" }
authors = [
  { name = "Pascal" }
]
requires-python = ">=3.8"
dependencies = []

[project.urls]
Homepage = "https://github.com/deinname/fastcoding"
