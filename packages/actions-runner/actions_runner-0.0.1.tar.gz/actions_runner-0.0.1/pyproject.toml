[project]
name = "actions-runner"
version = "0.0.1"
description = "Run GitHub Actions workflows locally in Python. Powered by `act` (nektosact.com)."
readme = "README.md"
requires-python = ">=3.11"
license = "MIT"
authors = [{"name"= "AJ Steers", "email"= "aaronsteers@example.com"}]
dependencies = []

[project.optional-dependencies]
dev = ["build", "twine"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
