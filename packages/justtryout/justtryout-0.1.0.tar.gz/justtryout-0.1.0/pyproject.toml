[project]
name = "justtryout"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "typer>=0.16.1",
]

[project.scripts]
justtryout = "justtryout.main:app"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
