[project]
name = "project-add"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "wangwenbin", email = "wangwenbin@gwm.cn" }
]
requires-python = ">=3.13"
dependencies = [
    "mcp[cli]>=1.13.0",
]

[project.scripts]
project-add = "project_add:main"

[build-system]
requires = ["uv_build>=0.8.11,<0.9.0"]
build-backend = "uv_build"
