[project]
name = "langchain-zeusdb"
version = "0.0.0"
description = "An integration package connecting ZeusDB and LangChain."

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"