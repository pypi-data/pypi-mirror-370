
[build-system]
requires = [
    "setuptools>=61.0",
    "wheel"
]
build-backend = "setuptools.build_meta"

[project]
name = "zafiro-api-provider"
version = "0.0.4"
description = "Zafiro API Provider"
dependencies = [
    "httpx"
]
requires-python = ">=3.8"
readme = "README.md"
license = {text = "MIT"}