# Nginx config file
# Copy it to /etc/sites-enabled or add it to default nginx.conf file
# Test server

server {
    listen 8888;
    server_name throttlebuster.com;

    location / {
        limit_rate 100k;  # Limit rate to 100 KB/s
        #root /home/smartwa/y2mate;  Set your directory
        index index.html;
    }
}
