[project]
name = "smallants-mcp-pypi"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "liruijiang", email = "2970053530@qq.com" }
]
requires-python = ">=3.13"
dependencies = [
    "mcp[cli]>=1.13.0",
]

[project.scripts]
smallants-mcp-pypi = "smallants_mcp_pypi:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
