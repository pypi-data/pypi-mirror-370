stages:
  - merge-request
  - tag

include:
  - local: '.gitlab-ci-merge-request.yml'
  - local: '.gitlab-ci-tag.yml'

merge_request_ci:bq:
  extends: .dbt_ci_template
  variables:
    PROJECT_PATH: "bq"

merge_request_ci:looker:
  extends: .dbt_ci_template
  variables:
    PROJECT_PATH: "looker"
