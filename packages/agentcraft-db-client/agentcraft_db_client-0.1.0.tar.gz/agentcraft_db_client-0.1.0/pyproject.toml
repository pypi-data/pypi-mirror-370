[project]
name = "agentcraft-db-client"
version = "0.1.0"
description = "Python SDK for a AgentCraft (Crafted) Vector DB"
readme = "README.md"
license = { file = "LICENSE" }
authors = [{ name = "Seyhun Akyurek", email = "info@we-crafted.com" }]
dependencies = ["requests"]

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"