[pytest]
markers =
    integration: marks tests that require real external services (Qdrant, Kuzu, GenAI)
    unit: marks unit tests
    adapter: marks adapter tests
    pipeline: marks pipeline tests
    api: marks API contract tests
    lifecycle: marks lifecycle tests
    edge_case: marks edge case tests
    plugin: marks plugin tests
