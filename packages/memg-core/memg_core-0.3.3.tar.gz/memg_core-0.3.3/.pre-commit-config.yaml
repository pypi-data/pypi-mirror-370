repos:
  # Basic file quality checks
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v4.6.0
    hooks:
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: check-yaml
      - id: check-json
      - id: check-toml
      - id: check-added-large-files
        args: ['--maxkb=1000']
      - id: check-merge-conflict
      - id: debug-statements

  # Python code formatting and linting with ruff
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.12.8
    hooks:
      # Run the linter first
      - id: ruff
        files: ^src/.*\.py$
        exclude: ^src/memory_system/version\.py$
        args: [--fix, --exit-non-zero-on-fix]
      # Then run the formatter
      - id: ruff-format
        files: ^src/.*\.py$
        exclude: ^src/memory_system/version\.py$

  # Security scanning with bandit
  - repo: https://github.com/PyCQA/bandit
    rev: 1.8.6
    hooks:
      - id: bandit
        files: ^src/.*\.py$
        args: [-c, bandit.yaml]

  # Type checking with mypy (disabled for now due to extensive structural issues)
  # - repo: https://github.com/pre-commit/mirrors-mypy
  #   rev: v1.17.0
  #   hooks:
  #     - id: mypy
  #       files: ^src/.*\.py$
  #       additional_dependencies: [types-requests, types-PyYAML, pydantic]
  #       args: [--ignore-missing-imports, --no-strict-optional]

  # Shell script linting - temporarily disabled due to SSL certificate issues
  # - repo: https://github.com/shellcheck-py/shellcheck-py
  #   rev: v0.10.0.1
  #   hooks:
  #     - id: shellcheck
  #       files: \.(sh|bash)$
