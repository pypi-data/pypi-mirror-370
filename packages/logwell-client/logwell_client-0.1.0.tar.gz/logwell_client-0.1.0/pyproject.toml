[project]
name = "logwell-client"
version = "0.1.0"
authors = [{ name = "Amirhossein Berenji", email = "ahberenji@gmail.com" }]
description = "A hassle-free client to send logs to LogWell-service"
readme = "README.md"
requires-python = ">=3.11"
dependencies = ["httpx>=0.28.1", "pydantic>=2.11.7"]

[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"
