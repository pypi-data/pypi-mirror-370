[tool.isort]
multi_line_output = 3
include_trailing_comma = true

[tool.pytest.ini_options]
addopts = "--tb=native -cov-report=term-missing -v"
python_files = "*_tests.py"


[build-system]
requires = ["setuptools>=42"]
build-backend = "setuptools.build_meta"
