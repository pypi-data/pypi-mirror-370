[project]
name = "catfile"
version = "1.0.2"
description = "A simple Python CLI tool to automatically sort files into directories by type"
readme = "README.md"
license = { text = "MIT" }
authors = [
  { name = "Jonathan", email = "reinishimiya22@gmail.com" }
]
dependencies = []
requires-python = ">=3.8"

[project.scripts]
catfile = "catfile.main:main"
