- name: Setup Prometheus, Grafana, and Node Exporter
  hosts: localhost
  become: true
  vars:
    grafana_url: "http://localhost:3001"
    domain: localhost
    http_port: 3001
  roles:
    - monitoring

 
    