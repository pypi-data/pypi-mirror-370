structure math
{
    static int pow(int base, int exp)
    {
        if (exp == 0) {
            return 1;
        }
        return base * math.pow(base, exp - 1);
    }

    static int fibonacci(int number)
    {
        if (number <= 2) {
            return 1;
        }
        return math.fibonacci(number - 1) + math.fibonacci(number - 2);
    }
}
