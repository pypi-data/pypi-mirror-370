[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "r00logger"
version = "0.1.5"
requires-python = ">=3.10"
dependencies = [
    "loguru",
    "decohints"
]

[tool.hatch.build.targets.wheel]
packages = ["src/r00logger"]