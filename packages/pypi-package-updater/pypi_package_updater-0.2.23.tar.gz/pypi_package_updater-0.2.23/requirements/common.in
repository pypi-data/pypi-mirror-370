# Core runtime dependencies for PyPI Updater (library-friendly)
aiohttp>=3.9,<4
packaging>=23.2
# Only needed on older Pythons; safe to drop if you require >=3.11
tomli>=2.0.1 ; python_version < "3.11"
# Backports for older Pythons; 3.12+ generally fine, but harmless.
typing-extensions>=4.11 ; python_version < "3.13"
