# Seccion que indica cómo construir tu paquete
[build-system]

# Lista de herramientas necesarias para la build
requires = ["setuptools", "wheel"]

# Define quien hace el build
build-backend = "setuptools.build_meta"
