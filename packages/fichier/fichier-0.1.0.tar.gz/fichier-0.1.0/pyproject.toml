[project]
name = "fichier"
version = "0.1.0"
description = "Outils pour fichiers, comme tree -a"
readme = "README.md"
requires-python = ">=3.6"
authors = [
    { name="TonNom", email="ton.email@example.com" }
]

[project.scripts]
fichier = "fichier.cli:main"

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
