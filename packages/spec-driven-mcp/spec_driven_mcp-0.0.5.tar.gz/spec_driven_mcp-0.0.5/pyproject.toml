[project]
name = "spec-driven-mcp"
version = "0.0.5"
description = "MCP server for specification-driven development: manage feature specs, requirements, acceptance criteria, test linkage and status."
authors = [ { name = "Tiago Melo" } ]
requires-python = ">=3.11"
readme = "README.md"
license = { text = "MIT" }
dependencies = [
  "mcp[cli]",
  "pydantic>=2.7",
  "uvicorn",
  "typer",
  "rich",
  "sqlite-utils>=3.36",
  "platformdirs"
]

[project.scripts]
spec-mcp-cli = "spec_mcp.server:main"
spec-mcp = "spec_mcp.server:main"
spec-mcp-stdio = "spec_mcp.mcp_main:main"

[tool.setuptools.packages.find]
include = ["spec_mcp*"]

[tool.uv]
managed = true

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"
