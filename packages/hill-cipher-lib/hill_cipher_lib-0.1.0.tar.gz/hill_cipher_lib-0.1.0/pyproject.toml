[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "hill-cipher-lib"
version = "0.1.0"
description = "A simple implementation of the Hill Cipher encryption and decryption."
authors = [{name = "Mokesh", email = "mokesh0606@gmail.com"}]
license = {text = "MIT"}
readme = "README.md"
requires-python = ">=3.7"
dependencies = ["numpy"]
