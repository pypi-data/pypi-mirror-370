[tool.poetry]
packages = [{include = "goedge", from = "src"}]

[project]
name = "goedge-core-sdk-python"
version = "0.1.1"
description = "GoEdge Core SDK Python Wrapper"
authors = [
    {name = "zhangtc",email = "zhangtc@rigoiot.com"}
]
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
