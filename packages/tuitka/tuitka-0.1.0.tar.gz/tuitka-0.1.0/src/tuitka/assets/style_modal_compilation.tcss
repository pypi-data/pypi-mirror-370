ModalScreen {
    align: center middle;

    Vertical {
        width: 95%;
        height: 85%;
        max-width: 200;
        max-height: 100;
        padding: 1;
        background: $surface;
    }

    .compilation-header {
        height: 3;
        text-align: center;
        margin: 0 0 1 0;
        color: $accent;
        text-style: bold;
    }

    .command-display {
        height: 4;
        width: 1fr;
        text-align: left;
        color: $text;
        margin: 0 0 1 0;
        padding: 1;
        background: $surface-lighten-1;
        border: solid $border;
        overflow-y: auto;
    }

    #compilation_terminal {
        height: 1fr;
        margin: 0 0 1 0;
    }

    OutputLogger {
        height: 1fr;
        border: solid $primary;
        margin: 0 0 1 0;
    }

    .compilation-status {
        height: 1;
        text-align: center;
        margin: 0 0 1 0;
    }

    .compilation-controls {
        height: 3;
        margin: 0;
    }

    .compilation-controls Button {
        width: 1fr;
        height: 3;
    }

    .success {
        color: $success;
        text-style: bold;
    }

    .error {
        color: $error;
        text-style: bold;
    }

    .in-progress {
        color: $warning;
        text-style: bold;
    }
}