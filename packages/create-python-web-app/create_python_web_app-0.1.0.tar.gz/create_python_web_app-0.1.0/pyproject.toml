[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "create_python_web_app"
version = "0.1.0"
description = "Python package to choose dynamically between flask, django, fastapi and have a folder structure ready"
readme = "README.md"
authors = [{name="dhiraj", email="dhirajjohare1497@gmail.com"}]
license = {text = "MIT"}
dependencies = []
