[project]
name = "hello-simple"
version = "0.1.0"
description = ""
authors = [{ name = "Asher", email = "jasherleslie@gmail.com" }]
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[project.scripts]
hello_simple = "hello_simple.main:main"

[tool.poetry]
packages = [{ include = "hello_simple", from = "src" }]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
