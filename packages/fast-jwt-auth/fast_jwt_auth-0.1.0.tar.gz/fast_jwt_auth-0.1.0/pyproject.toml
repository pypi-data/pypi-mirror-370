[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "fast-jwt-auth"
version = "0.1.0"
authors = [
  { name="romeo", email="romeomanoela18@gmail.com" },
]
description = "Une bibliothèque d'authentification JWT simple pour FastAPI."
readme = "README.md"
requires-python = ">=3.8"
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
    "Framework :: FastAPI",
]
dependencies = [
    "build>=1.2.2.post1",
    "PyJWT>=2.0.0",
    "setuptools>=61.0",
    "twine>=6.1.0",
]

[project.urls]
"Homepage" = "https://github.com/romeo/fast-jwt"
"Bug Tracker" = "https://github.com/romeo/fast-jwt/issues"

[project.optional-dependencies]
test = [
    "pytest",
    "httpx",
    "uvicorn",
]
