{
  "command": "clean_faiss_orphans",
  "summary": "Clean FAISS orphan records",
  "description": "Remove orphaned records from FAISS index that have no corresponding metadata in Redis",
  "parameters": {},
  "required": [],
  "examples": {
    "clean_orphans": {
      "description": "Clean FAISS orphan records",
      "request": {
        "jsonrpc": "2.0",
        "method": "clean_faiss_orphans",
        "id": 1
      }
    }
  }
} 