{
  "command": "find_duplicate_uuids",
  "summary": "Find all UUIDs with duplicate records and return their metadata",
  "description": "Find and return UUIDs that have duplicate records in the vector store",
  "parameters": {
    "metadata_filter": {
      "type": "object",
      "description": "Optional metadata filter to apply before finding duplicates",
      "additionalProperties": true
    }
  },
  "required": [],
  "examples": {
    "find_all_duplicates": {
      "description": "Find all duplicate UUIDs",
      "request": {
        "jsonrpc": "2.0",
        "method": "find_duplicate_uuids",
        "id": 1
      }
    },
    "find_duplicates_with_filter": {
      "description": "Find duplicate UUIDs with metadata filter",
      "request": {
        "jsonrpc": "2.0",
        "method": "find_duplicate_uuids",
        "params": {
          "metadata_filter": {
            "category": "test",
            "language": "en"
          }
        },
        "id": 1
      }
    }
  }
} 