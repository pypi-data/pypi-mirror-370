[project]
name = "watchgate-gateway"
version = "0.1.0"
description = "Secure MCP gateway with auditing and access control (core gateway without TUI)"
readme = "README.md"
license = {text = "Apache-2.0"}
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"