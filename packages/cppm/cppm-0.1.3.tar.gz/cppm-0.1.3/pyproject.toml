[project]
name = "cppm"
version = "0.1.3"
description = "A CLI helper tool for managing C++ projects"
authors = [
    {name = "Kape",email = "kleberkapelinski@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.9"
license = "MIT"
dependencies = [
]


[tool.poetry.scripts]
cppm = 'cppm.__init__:main'

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

