[project]
name = "ewha_cong_dynamicP"                
version = "0.1.0"                  
description = "A Python module for calculating and analyzing three types of congestion—Road, PM, and Pedestrian congestion."
authors = [
    { name="Jiyoon Lee", email="0197black@gmail.com" }  
]
readme = "README.md"                
requires-python = ">=3.8"          
dependencies = [
    "numpy",
    "pandas",
    "geopandas",
    "movingpandas",
    "tqdm",
    "shapely",
    "geopy"
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = { find = { where = ["src"] } }

