{
  "mcpServers": {
    "验证码生成工具": {
      "command": "python",
      "args": [
        "-c",
        "from mcp_ts.server import create_and_run_server; create_and_run_server(name='VerificationCodeServer', description='验证码生成服务器')"
      ]
    }
  }
}