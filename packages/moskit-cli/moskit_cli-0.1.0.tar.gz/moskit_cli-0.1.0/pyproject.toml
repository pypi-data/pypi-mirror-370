[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "moskit-cli"
version = "0.1.0"
description = "Client for MOSKIT controller (Home / Position / Velocity)"
readme = "README.md"
requires-python = ">=3.8"
license = {text = "MIT"}
authors = [{ name = "Ensume" }]
keywords = ["moskit", "teensy", "motor", "cli", "api"]
dependencies = []

[project.urls]
Homepage = "https://ensume.com"
