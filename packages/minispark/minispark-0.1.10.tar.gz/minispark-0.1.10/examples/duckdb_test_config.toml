# 本地处理引擎配置（专门用于测试DuckDB）
[engine]
# 引擎类型，支持 duckdb 或 sqlite
type = "sqlite"
# 数据库路径，支持两种类型：
# 1. 内存数据库 - 使用 ":memory:" 
# 2. 临时文件数据库 - 使用相对路径（相对于系统临时目录）的文件名，如 "minispark.db"
database_path = ":memory:"

# 临时数据存储配置
[storage]
# 存储格式，支持 parquet 或 avro
format = "parquet"