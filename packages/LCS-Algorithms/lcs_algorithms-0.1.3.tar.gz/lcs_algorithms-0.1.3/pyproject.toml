[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "LCS_Algorithms"
version = "0.1.3"
description = "A collection of LCS algorithms in Python"
readme = "README.md"
authors = [
    { name = "Zeshan Khan", email = "zeshankhanalvi@gmail.com" }
]
license = { text = "MIT" }
requires-python = ">=3.8"
dependencies = []

[project.urls]
Homepage = "https://github.com/zeshanalvi/lcs_algorithms"