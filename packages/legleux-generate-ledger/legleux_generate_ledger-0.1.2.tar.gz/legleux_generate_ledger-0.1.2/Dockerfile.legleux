FROM debian:trixie-20250721-slim

RUN apt-get update && apt-get upgrade -y && apt-get install -y \
    curl \
    ca-certificates \
    gpg \
    curl \
    wget \
    jq \
    vim \
    cmake \
    git \
    python3-venv \
    python3-pip
