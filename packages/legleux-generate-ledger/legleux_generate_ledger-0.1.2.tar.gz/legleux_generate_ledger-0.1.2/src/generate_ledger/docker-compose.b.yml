version: "3.9"
name: butts

services:
  # validators
  app:
    container_name: val
    image: legleux/rippled:latest
    # command: ["python","-m","myapp"]
    environment:
      PYTHONUNBUFFERED: "1"
      PYTHONDONTWRITEBYTECODE: "1"
      # APP_ENV: development
    # ports:
      # - "8000:8000"
