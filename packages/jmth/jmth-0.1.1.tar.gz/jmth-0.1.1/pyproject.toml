[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "jmth"
version = "0.1.1"
description = "Simple math library: volume cube & solve SPLDV"
authors = [
  { name="Galih Julian", email="membventra@gmail.com" }
]
license = { text="MIT" }
readme = "README.md"
requires-python = ">=3.7"
