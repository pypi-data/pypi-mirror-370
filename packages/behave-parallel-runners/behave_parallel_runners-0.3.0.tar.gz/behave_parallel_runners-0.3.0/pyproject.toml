[project]
name = "behave-parallel-runners"
version = "0.3.0"
description = ""
authors = [{name = "Lokkina Olesia"}]
license = "MIT"
requires-python = "<4.0,>=3.13"
dependencies = [
    "behave (>=1.3.0,<2.0.0)"
]

[tool.poetry]
packages = [{include = "behave_parallel_runners"}]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
