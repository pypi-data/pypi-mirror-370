[project]
name = "sccb"
version = "0.3.0"
description = "Shortcut Clipboard + Command Binder"
authors = [{ name = "Ajinkya Dhamdhere" }]
dependencies = ["typer", "pyperclip", "rich"]

[project.scripts]
sccb = "sccb.cli:app"