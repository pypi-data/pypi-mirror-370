[build-system]
requires = ["setuptools>=61", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "my-mongo-mcp"
version = "0.1.1"
description = "Fork of mongo-mcp"
readme = "README.md"
authors = [{name="lh", email="1019914263@qq.com"}]
license = {text = "MIT"}
requires-python = ">=3.8"
dependencies = [
    "pymongo>=4.0",
    "mcp>=0.6.0",
]
[project.scripts]
my-mongo-mcp = "my_mongo_mcp.cli:main"
[project.urls]
Homepage = "https://github.com/jonfreeland/mongodb-mcp"
