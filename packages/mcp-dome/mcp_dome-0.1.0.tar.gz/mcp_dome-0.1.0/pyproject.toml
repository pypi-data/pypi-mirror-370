[project]
name = "mcp-dome"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "map[cli]>=1.3.0",
]

[project.scripts]
mcp-dome = "mcp_dome:main"

[build-system]
requires = ["uv_build>=0.8.11,<0.9.0"]
build-backend = "uv_build"
