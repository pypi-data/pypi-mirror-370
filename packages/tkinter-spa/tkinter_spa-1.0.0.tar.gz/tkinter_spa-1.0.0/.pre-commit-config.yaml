# Run with pre-commit run --all-files

repos:
  - repo: local
    hooks:
      - id: ruff-check
        name: Ruff
        entry: ruff check . --fix --unsafe-fixes --config ruff.toml
        language: system
        pass_filenames: false
